import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["i.pinimg.com", "example.com", "res.cloudinary.com"], // Add the domain here
  },
};

export default nextConfig;
